# /etc/systemd/system/fake-hwclock.service
[Unit]
Description=Restore and persist clock without RTC (fake-hwclock)
DefaultDependencies=no
Before=sysinit.target
Conflicts=shutdown.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/opt/pim/bin/fake-hwclock.sh load
ExecStop=/opt/pim/bin/fake-hwclock.sh save
Environment="LOG_TO_KERNEL=1"

[Install]
WantedBy=sysinit.target
