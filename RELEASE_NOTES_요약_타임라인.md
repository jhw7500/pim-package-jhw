# PIM Package 주요 개선 타임라인
## v0.5.4 → v0.5.7 (2025-08 ~ 2026-01)

---

## 🎯 버전별 핵심 요약

| 버전 | 날짜 | 핵심 키워드 | 주요 변경 |
|------|------|------------|-----------|
| **v0.5.4.1** | 08-27 | 🧪 실험 | 인코더 설정 테스트 |
| **v0.5.4** | 09-18~22 | 📸 캡처 품질 | gstApp v1.1 + SD 서비스 |
| **v0.5.5** | 09-23~10-28 | 🛡️ 안정성 | ORD 복사예측 + RTC + 카메라 |
| **v0.5.6.1** | 11-14~26 | 💾 ext4 최적화 | ext4 마운트 옵션 + journald 통합 |
| **v0.5.7** | 12-01~01-09 | ⚡ 성능 혁명 | 비동기 캡처 + Request Queue |

### 간단 요약
- **v0.5.4**: 캡처 품질 제어 가능
- **v0.5.5**: 네트워크/RTC/카메라 안정화
- **v0.5.6.1**: 💥 ext4 마운트 최적화 + journald (가장 큰 변화)
- **v0.5.7**: 💥 비동기 캡처로 성능 대폭 향상 (가장 큰 변화)

---

## 🎯 4단계 진화 과정

### 1단계: 캡처 기능 강화 (v0.5.4.1 ~ v0.5.4)
**기간**: 2025-08-27 ~ 2025-09-22 | **커밋**: 6개

**목표**: 이미지 캡처 품질 제어 및 모니터링 강화

- **gstApp 인코더/품질 설정**
  - 이미지 캡처 시 인코더 형식 선택 가능 (JPEG, JPEG Turbo, RAW)
  - 캡처 품질을 0~100 범위로 조절 가능
  - edgeconf 파일에서 설정 가능하도록 확장

- **스트림 watchdog**
  - 비디오 스트리밍 상태를 실시간 모니터링
  - 스트리밍 중단 시 자동 감지 및 복구 준비

- **SD 카드 서비스 도입**
  - `sd-mount.service` systemd 서비스 추가
  - SD 카드 자동 마운트/언마운트 관리
  - 마운트 실패 시 재시도 로직 포함

---

### 2단계: 시스템 안정성 (v0.5.5)
**기간**: 2025-09-23 ~ 2025-10-28 | **커밋**: 8개

**목표**: 네트워크 고부하 환경 대응 및 시스템 초기화 개선

- **ORD v4.6 이벤트 복사 시간 예측**
  - 높은 bps 영상 파일의 이벤트 복사 시 복사 지연 시간 자동 계산
  - `evt_copy_delay = (max_bps/1024) * recMinute` 공식 적용
  - 높은 화질 영상의 큰 용량으로 인한 복사 지연 문제 해결

- **SD 카드 읽기 전용 자동 복구** ⚠️ (v0.5.7에서 제거)
  - `/sys/block/mmcblk1/ro` 파일로 읽기 전용 상태 자동 감지
  - 감지 시 자동으로 fsck.vfat 실행하여 복구 시도
  - 복구 후 자동 재마운트
  - (참고: 이 기능은 시스템 불안정성을 유발하여 v0.5.7에서 제거됨)

- **RTC 빠른 로딩**
  - Real-Time Clock 초기화 시간 대폭 단축
  - 부팅 직후부터 정확한 타임스탬프로 로깅 가능
  - fake-hwclock 추가로 RTC 하드웨어 없어도 시간 유지

- **카메라 회전 기능 재활성화**
  - 2024년에 제거되었던 streamApp 카메라 회전 기능 복구
  - 카메라 영상 회전 각도 설정 (0°, 90°, 180°, 270°)
  - i2c를 통한 하드웨어 레벨 rotate_bit 제어
  - 카메라 물리적 설치 방향에 따라 소프트웨어로 보정

---

### 3단계: 스토리지 혁신 (v0.5.6.1)
**기간**: 2025-11-14 ~ 2025-11-26 | **커밋**: 11개

**목표**: SD 카드 안정성 극대화 및 로그 시스템 전면 개편

- **SD 카드 ext4 마운트 최적화**
  - 파일시스템 타입별 최적화된 마운트 옵션 적용
  - ext4: commit=60, data=writeback 등 성능 옵션 적용
  - FAT32/exfat: 기존 옵션 유지
  - SD 카드 읽기 전용 모드 자동 감지 및 복구

- **동적 파일시스템 감지 및 최적화**
  - SD 카드 삽입 시 자동으로 FAT32/ext4/exfat 타입 판별
  - 파일시스템 타입에 맞는 최적 마운트 옵션 자동 적용
  - 모든 파일시스템 타입에 대한 호환성 유지

- **journald 시스템 개편**
  - systemd-journald를 통한 통합 로그 관리
  - RAM 사용량 제한 설정으로 메모리 효율화
  - 스냅샷 기능으로 중요 로그를 eMMC에 영구 보관
  - timer에서 crontab으로 전환하여 안정성 향상

---

### 4단계: 성능 최적화 (v0.5.7)
**기간**: 2025-12-01 ~ 2026-01-09 | **커밋**: 12개

**목표**: 빠른 연속 캡처 지원 및 ext4 성능 튜닝

- **gstApp v1.2 비동기 캡처**
  - 캡처 작업을 별도 워커 쓰레드에서 처리
  - 메인 애플리케이션 블로킹 없이 캡처 가능
  - 캡처 중에도 다른 명령 즉시 응답

- **Request Queue 아키텍처**
  - 연속된 캡처 명령을 큐(대기열)에 저장
  - 캡처 요청이 빠르게 들어와도 순서대로 처리
  - 명령 누락 없이 모든 캡처 요청 처리 보장
  - 큐 오버플로우 방지 로직 추가

- **SD 카드 ext4 최적화 및 안정화**
  - ext4 커밋 주기 튜닝으로 쓰기 성능 향상
  - ext4_downgrade.sh, chk_filesystem.sh 관리 도구 추가
  - SD 카드 마운트 실패 시 임시 경로로 자동 폴백

- **SD 카드 복구 전략 변경**
  - v0.5.5의 자동 fsck 복구 로직 제거
  - 자동 복구가 시스템 불안정성을 유발하여 제거 결정
  - 수동 복구 도구로 대체 (ext4_downgrade.sh, chk_filesystem.sh)
  - SD 마운트 실패 시 `/dev/shm`으로 자동 폴백하여 서비스 중단 방지
  - edgeconf의 tmp_path를 자동으로 `/dev/shm`으로 변경하여 재시작

- **ORD v4.7 에러 리포팅 제어**
  - `err_send_period` 설정으로 에러 전송 주기 제어
  - 네트워크 트래픽 최적화 (불필요한 에러 중복 전송 방지)
  - 에러 발생 시 서버 전송 간격을 초 단위로 설정 가능

---

## 📊 단계별 비교

| 단계 | 버전 | 기간 | 핵심 키워드 |
|------|------|------|-------------|
| 1단계 | v0.5.4 | 1개월 | 캡처 품질, 모니터링, SD 서비스 |
| 2단계 | v0.5.5 | 1개월 | 네트워크 안정성, RTC, 카메라 |
| 3단계 | v0.5.6.1 | 13일 | **ext4 최적화**, 로그 혁신 |
| 4단계 | v0.5.7 | 1.3개월 | **비동기**, 큐, 성능 |

---

## 🔄 발전 흐름도

```
[v0.5.4.1] 캡처 품질 제어
    ↓
[v0.5.4] SD 카드 자동화 + 스트림 감시
    ↓
[v0.5.5] 시스템 안정성 (네트워크/RTC/카메라)
    ↓
[v0.5.6.1] 💥 ext4 마운트 최적화 + journald
    ↓
[v0.5.7] 💥 성능 혁명 (동기 → 비동기 캡처)
```

---

## 💡 핵심 기술 설명

### Request Queue 아키텍처란?
**문제**: 캡처 명령이 0.1초 간격으로 10개 들어오면 기존에는 일부 누락됨

**해결**:
```
[명령1] [명령2] [명령3] [명령4] ...
   ↓       ↓       ↓       ↓
  [Queue 대기열]
   ↓
[Worker Thread]
   ↓
캡처 1 → 캡처 2 → 캡처 3 → ... (순차 처리)
```

- 명령을 큐에 쌓아두고 순서대로 처리
- 빠른 연속 요청도 누락 없이 모두 처리
- 큐가 가득 차면 오류 반환 (무한 대기 방지)

### ext4 vs FAT32
| 구분 | FAT32 | ext4 |
|------|-------|------|
| 저널링 | ✗ | ✓ (파일 손상 방지) |
| 파일 크기 제한 | 4GB | 16TB |
| 파일 개수 제한 | 제한적 | 거의 무제한 |
| 안정성 | 낮음 | 높음 |
| 읽기 전용 복구 | 어려움 | 자동 감지 |

### 비동기 캡처의 이점
**기존 (동기)**:
```
명령 수신 → 캡처 시작 (0.5초) → 완료 → 다음 명령 수신
          [메인 앱 블로킹]
```

**개선 (비동기)**:
```
명령 수신 → 즉시 응답 (0.001초)
            ↓
         [Worker Thread]
         캡처 시작 (0.5초) → 완료

메인 앱은 다른 작업 계속 수행 가능
```

---

## ⚠️ 버전별 필수 조치사항

### v0.5.5 → v0.5.6.1 업그레이드
🟢 **권장**: ext4 SD 카드 사용 시 최적화된 마운트 옵션 적용 (FAT32/exfat도 계속 사용 가능)

### v0.5.6.1 → v0.5.7 업그레이드
🟡 **필수**: ORD 설정 파일에 추가
```json
{
  "err_send_period": 60
}
```

---

**작성일**: 2026-01-09
**작성자**: hwjo
**총 개발 기간**: 4.5개월
**총 커밋**: 37개
